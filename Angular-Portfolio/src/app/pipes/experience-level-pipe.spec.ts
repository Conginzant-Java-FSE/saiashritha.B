import { ExperienceLevelPipe } from './experience-level-pipe';

describe('ExperienceLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
